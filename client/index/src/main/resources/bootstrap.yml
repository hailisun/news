spring:
  cloud:
    config:
      name: index,database,redis
      label: master
      discovery:
        enabled: true
        serviceId: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/
#server:
#  port: 8081
